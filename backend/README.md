# Backend

Planned Python + FastAPI service.

This package will own the API contract, authentication boundary, catalog access, cart and order state, Razorpay integration, webhook verification, and the bounded agent entrypoint.